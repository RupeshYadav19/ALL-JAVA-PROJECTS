package dao;

import db.DBConnection;
import models.Vendor;
import java.sql.*;
import java.util.ArrayList;
import java.util.List;

/**
 * Extension of VendorDAO — adds getAll() and other helpers referenced from panels.
 * NOTE: The main VendorDAO already has core CRUD. This file adds stubs that delegate.
 */
public class VendorDAOExtra {
    // This is just a note — all methods are already in VendorDAO.java
}
